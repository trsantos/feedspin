<li>
  <% if subscription.starred? %>
	<% status = "★ " %>
  <% else %>
	<% status = "" %>
  <% end %>

  <%= link_to status + sub_title(subscription.feed),
              subscription.feed,
              class: subscription.updated? ? "updated-sub" : "" %>
</li>
